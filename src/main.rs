// TODO: Remove these allows as modules get wired up in later stories (1.3+).
// They are here to keep the scaffold clean while types are being defined.
#![allow(dead_code)]
#![allow(unused_imports)]

mod config;
mod engine;
mod input;
mod tui;

fn main() {
    println!("Hello, world!");
}
